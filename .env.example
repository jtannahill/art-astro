# Public Mapbox client token (pk.*). Used by /map/ at build time and
# embedded in the rendered HTML. Same token production exposes.
# Local dev: copy this file to .env and fill in.
# CI: set as repo secret PUBLIC_MAPBOX_TOKEN.
PUBLIC_MAPBOX_TOKEN=pk.your_mapbox_public_token_here
